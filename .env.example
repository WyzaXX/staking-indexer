# Database connection
DB_NAME=staking
DB_PORT=5433
DB_HOST=localhost
DB_USER=postgres
DB_PASS=postgres
GQL_PORT=4350

# Chain configuration
CHAIN=moonbeam
CHAIN_RPC_ENDPOINT=https://rpc.api.moonbeam.network

# Total supply for Moonbeam (1.2B GLMR with 18 decimals)
TOTAL_SUPPLY=1200000000000000000000000000

# SS58 prefix for Moonbeam
SS58_PREFIX=1284

# Block range (optional - start from a recent block for faster testing)v
START_BLOCK=0
# END_BLOCK=

# Connection String for other external tools:
CONNECTION_STRING=postgresql://postgres:postgres@localhost:5433/staking